[tool.poetry]
name = "loading"
version = "0.1.0"

[tool.poetry.dependencies]
python = ">3.10"
pandas = "*"
numpy = "*"
requests = "*"
matplotlib = "*"
dotenv = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
